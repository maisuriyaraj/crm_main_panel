# Spec: [Feature or Improvement Name]

## Status
See specs/WORKFLOW.md "Status Convention" for the exact allowed values.

## Type
New Feature / Improvement / Bug Fix / Refactor

## Goal
One or two sentences. What does this solve or improve?

## Current State
- How does it work today?
- What are the known problems or limitations?
- Relevant existing files

## Proposed Change
- What exactly will change?
- Why this approach?

## Affected Files
List every file that will be created or modified.

## Design (include only if this spec involves UI changes)
- Screens or components affected
- Existing components being reused
- New components needed, if any
- Design tokens in use: colours, typography, spacing
- Reference screens to match
- Platform differences if any (web vs mobile)

## AI Prompts (include only if this spec involves AI calls)
- Prompt v1: paste exact prompt here
- Prompt v2: updated version if changed

## API Changes (include only if endpoints are added or changed)
- Endpoint, method, request shape, response shape

## Database Changes (include only if schema is changing)
- New tables or columns
- Migration file name

## Risks
- What could break?
- What needs manual testing?

## Open Questions
- Anything not yet decided

## Implementation Notes
- Filled after implementation
- Keep it brief. One or two lines for small fixes.
- What was done, any deviations from plan, any follow-up tasks
- Skip this section entirely if there is nothing meaningful to say

## Change Request History
- Filled by /sdd-change when requirements change after implementation
- Format: [Date] | [What changed] | [Why] | [Requested by]
- Keep each entry short. If this section grows very long over many
  revisions, it may be trimmed during a /sdd-cleanup pass, but only
  when an entry is fully superseded and safe to condense.
